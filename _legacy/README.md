# Legacy artifacts

Material kept for reference or for the v2 shell installer (`cursor-handoff install` / `list`).

- **`v2/`** — install-on-chat docs, manual skills registry JSON, v2 `plugin.json`, and `build-registry.sh`.

Primary distribution for new projects: `npx agent-kit init` and `packages/cli` (v3).
