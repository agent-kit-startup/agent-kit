---
description: Regras CURSOR-SKILLS para DevOps (Docker, Kubernetes, CI/CD, Terraform, IaC).
globs: "**/Dockerfile,**/docker-compose*.{yml,yaml},**/.github/**/*.yml,**/.gitlab-ci.yml,**/*.yml,**/k8s/**/*,**/*.tf,**/*.tfvars"
alwaysApply: false
---

# CURSOR-SKILLS – DevOps

Fonte: [cursor-skills/devops](https://github.com/araguaci/cursor-skills/tree/main/devops)

## Setup CURSOR

- Docker, Kubernetes, GitLens, Remote Development, YAML, Terraform.

## Estrutura típica

```
project/
├── docker/   ├── k8s/   ├── terraform/   ├── .github/
└── .env
```

## Docker

- Dockerfile e docker-compose; multi-stage builds; cache de layers; segurança (usuário não-root, imagens mínimas).

## Kubernetes

- Pods, Services, Deployments; ConfigMaps e Secrets; recursos e limites; health checks; convenções de nome.

## CI/CD

- Pipelines (GitHub Actions, GitLab CI, etc.); estágios (lint, test, build, deploy); triggers; notificações; rollback.
- Quando o projeto adotar fluxo Git staging → produção: usar `git staging` e `git prod`; nunca commit direto em `main`. Ver regra [cursor-skills-git-workflow.mdc](cursor-skills-git-workflow.mdc).

## Spine DevOps: Git staging → prod

O fluxo de dois degraus (staging → main) é a memória operacional do kit. Reforços que evitam vazar trabalho direto pra produção:

1. **Staging-first sempre.** Trabalho e commits acontecem em branch de trabalho ou em `staging`, nunca com o checkout em `main`.
2. **Voltar de `main` após `git prod`.** Promoção deixa o checkout em `main`; ao terminar, fazer `git checkout staging`. Ficar em `main` é o jeito mais comum de um commit escapar pra linha de produção.
3. **Podar branches legadas.** Branch de pré-prod antiga (`homologacao`, `develop`) que já foi migrada pra `staging` deve ser removida (local e remotos) pra não confundir o fluxo nem o histórico.
4. **Enforcement, não só convenção.** Instalar as guardas em `git-hooks/` (`pre-commit` e `pre-push`) que abortam commit/push direto em `main`/`master`. Instalação em [git-hooks/README.md](git-hooks/README.md).
5. **Auditar a linha do `main`.** `git log --first-parent --no-merges main` deve retornar só o commit raiz. Qualquer outro non-merge ali é um commit que nasceu direto em `main` (violação do fluxo).

Detalhes e prompts em [autogit/gitupdate.md](autogit/gitupdate.md); regra completa em [cursor-skills-git-workflow.mdc](cursor-skills-git-workflow.mdc).

## IaC (Terraform)

- Módulos; versionamento; state remoto; `terraform fmt`; documentação de recursos e variáveis.

## Boas práticas

- Logs, env vars, secrets fora do código; testes de infra quando possível; docs de deploy e troubleshooting.
