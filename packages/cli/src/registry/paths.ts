/**
 * Shared relative paths for registry-owned kit files.
 * POSIX literals so error messages and joins stay stable across OS.
 */

/** Index file under a registry root (`registry/registry.json`). */
export const REGISTRY_INDEX_REL = "registry/registry.json";
