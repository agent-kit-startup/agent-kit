/**
 * Shared relative paths for scanner-owned project files.
 * POSIX literals so evidence labels and change records stay stable across OS.
 */

/** Repository profile written by safe-fixes / read by detect-* confirmed lanes. */
export const REPOSITORY_PROFILE_REL = ".cursor/agent-kit.config.json";
